# Config locale per scripts/deploy-pantheon.sh — copia in scripts/.pantheon.env (NON tracciato).
# Il segreto (basic auth) NON va committato.
PANTHEON_BASIC_AUTH="utente:password"            # credenziali della Lock Pantheon
# Opzionali (hanno già un default sensato nello script):
# PANTHEON_SITE_URL="https://dev-mariani-test-webiste.pantheonsite.io"
# PANTHEON_GIT_URL="ssh://codeserver.dev.<uuid>@codeserver.dev.<uuid>.drush.in:2222/~/repository.git"
# PANTHEON_SSH_KEY_FILE="$HOME/.ssh/pantheon_deploy"
